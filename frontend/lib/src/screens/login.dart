import 'package:flutter/material.dart';

// ログイン画面表示
class LoginPage extends StatelessWidget {
  // コンストラクタ
  const LoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('ログイン画面表示')),
    );
  }
}
